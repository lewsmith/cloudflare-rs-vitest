{
  "name": "cloudflare-rs-vitest",
  "version": "1.0.0",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.2.6",
    "@cloudflare/workers-types": "^4.20240423.0",
    "@types/node": "^20.12.8",
    "typescript": "^5.4.5",
    "vitest": "^1.3.0",
    "wrangler": "^3.53.1"
  },
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "test": "vitest -c vitest.config.ts --reporter basic"
  }
}
